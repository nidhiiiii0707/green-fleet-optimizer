pandas==3.0.5
numpy==2.5.3
joblib==1.6.0
scikit-learn==1.8.0
# IMPORTANT: xgboost MUST be 3.0.0, NOT 3.4.1.
# models/fuel_xgb_pipeline.joblib embeds a Booster serialized under
# XGBoost 3.0.x's internal buffer format. Loading it under 3.4.1 (and only
# 3.4.1 was tested/confirmed broken; 3.1.0 and 2.1.4 also loaded correctly)
# raises: xgboost._c_api.XGBoostError: input stream corrupted
# This is a LOADING/ENVIRONMENT version-compatibility issue, not a corrupted
# file (verified byte-identical, same md5, against the sibling project's copy
# of the same model). Do not retrain; just pin this version.
xgboost==3.0.0
scipy==1.18.1
matplotlib==3.11.2
pymoo==0.6.2
pytest==9.1.1
# CQM (cqm_model.py/cqm_solver.py) formulation + classical (non-quantum)
# ExactCQMSolver reference solve; no D-Wave Leap/cloud token used or required.
dimod==0.12.22
# backend/ (FastAPI dashboard API) — not previously declared here.
fastapi==0.141.1
uvicorn==0.53.0
httpx==0.28.1
