[pytest]
# test_candidate.py, test_candidate_evaluation.py, test_feasibility.py,
# test_fuel_predictor.py, test_loaders.py, test_objectives.py,
# test_parameter_builder.py, test_smoke.py are orphaned copies from the
# sibling green-fleet-optimizer-with-optimization project (they import a
# `src.*` package that does not exist in this repo) -- not part of this
# repo's data layer or this task's deliverables, and were pre-existing on
# disk before this run. They are excluded here rather than deleted.
addopts = --ignore=tests/test_candidate.py --ignore=tests/test_candidate_evaluation.py --ignore=tests/test_feasibility.py --ignore=tests/test_fuel_predictor.py --ignore=tests/test_loaders.py --ignore=tests/test_objectives.py --ignore=tests/test_parameter_builder.py --ignore=tests/test_smoke.py
